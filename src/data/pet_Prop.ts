import Pet from '../data/pet'
import PetImg from './pet-image';
import { PetImageProps } from './pet_Image_Prop';

export default interface petProp {
    petObj : Pet
    petImageObj : PetImageProps
}
